# Expo/React Native + Metro need a flat node_modules to resolve transitive
# deps (expo-modules-core, @babel/runtime, etc.). pnpm's default isolated
# layout breaks Metro resolution in a monorepo, so use a hoisted layout.
node-linker=hoisted
